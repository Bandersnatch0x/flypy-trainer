<view class="page">
  <view class="head">
    <text class="title">数据来源与许可</text>
    <text class="lead">鹤练是纯本地离线工具：不收集、不上传任何数据。内置字码数据来自开源 Rime 输入法生态，按其许可证随包分发；每份数据包内嵌出处与许可头。以下为出处、许可与改动说明（LGPL 义务：保留声明、注明出处、标注改动；CC-BY-4.0 / 政府资料开放授权义务：署名）。</text>
  </view>

  <view wx:for="{{sources}}" wx:key="title" class="card src">
    <view class="srchead">
      <text class="sname">{{item.title}}</text>
      <text wx:if="{{item.license}}" class="lic">{{item.license}}</text>
      <text wx:else class="lic own">自写</text>
    </view>
    <text class="sbody">{{item.body}}</text>
    <view wx:if="{{item.upstream}}" class="url" data-url="{{item.upstream}}" bindtap="copyUrl" hover-class="url-hover">
      <text class="utext mono">{{item.upstream}}</text>
      <text class="ucopy">复制</text>
    </view>
  </view>

  <view class="card thanks">
    <text class="sname">致谢</text>
    <text class="sbody">{{thanks}}</text>
    <view wx:for="{{licenseUrls}}" wx:key="name" class="url" data-url="{{item.url}}" bindtap="copyUrl" hover-class="url-hover">
      <text class="utext">{{item.name}} 全文</text>
      <text class="ucopy">复制</text>
    </view>
  </view>

  <view class="foot">链接不能在小程序内直接打开，点按可复制到剪贴板。</view>
</view>
