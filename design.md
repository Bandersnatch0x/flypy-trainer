# Design System: 鹤练 FlypyTrainer（小鹤双拼练习站）

> 来源：stitch-design-taste 方法论（Stitch MCP 未挂载，人工执行）。
> 定稿前已通过反模式评审（见文末评审记录）。

## 1. Visual Theme & Atmosphere

深夜书法房的气质：墨色底、一点朱砂。练习时注意力必须钉在中央目标字上，
其余一切退为低语。密度 4（日常应用平衡档）、变化度 6（练习主屏严格中轴对称——
它是"工具仪表"不是 Hero 着陆页，对称是刻意决定而非懒惰）、动效 4（流体但克制，
打字是高频微交互，动画绝不能拖节奏）。桌面优先；<768px 单列降级、键盘图隐藏
（移动端仅浏览课程与统计）。

## 2. Color Palette & Roles

- **Ink Canvas** (`#101014`) — 全站背景，近黑的墨色（非纯黑 #000000）
- **Ink Surface** (`#17171C`) — 卡片/键盘键帽底面
- **Ink Raised** (`#1F1F26`) — 悬停/激活键帽
- **Whisper Line** (`rgba(255,255,255,0.08)`) — 1px 分隔线、键帽描边
- **Bone Text** (`#EDEDEF`) — 主文字（目标字、标题）
- **Ash Text** (`#8B8B93`) — 次级文字、提示、元数据
- **Cinnabar** (`#D96C4F`) — 唯一强调色：当前要按的键 ①、正确反馈、主按钮。饱和度约 63%
- **Bamboo** (`#7FA98C`) — 唯一辅助色：已完成键 ②、正确计数、进度条。低饱和灰绿
- **Rust** (`#C4574E`) — 错误反馈、错键热力高值（与 Cinnabar 同族不刺眼）

禁令：无紫/蓝霓虹、无渐变按钮辉光、全站无第二种高饱和色。

## 3. Typography Rules

- **目标字（Display）**：`"Source Han Serif SC", "Noto Serif SC", serif` 思源宋体，
  字重 600，`clamp(4rem, 12vw, 7rem)`——练习的焦点必须是汉字本体，宋体笔锋
  帮助零基础者看清结构；这是全站唯一衬线使用，属教学用途而非装饰。
- **UI 文字（Body）**：`"Outfit", "PingFang SC", "Microsoft YaHei", sans-serif`，
  行高 1.6，正文最大 65ch。Outfit 经 Google Fonts 引入（唯一外链）。
- **码与数字（Mono）**：`"JetBrains Mono", monospace`——小鹤码、统计数字、
  计时器一律等宽（密度>7 的区域：统计页）。
- 键帽字母：JetBrains Mono 600，18px。
- 禁令：无 Inter、无花哨渐变字。

## 4. Component Stylings

- **键帽 Key**：48×48px，圆角 10px，Ink Surface 底 + Whisper Line 描边。
  状态：默认 / ①待按（Cinnabar 填充 + 右上 ① 角标）/ ②待按（Bamboo 描边 + ②）/
  按下（-2px translate + 亮度+6%，仅 transform）/ 错按（Rust 闪烁 120ms）。
  键帽下方小字注韵母（灰色 10px）。
- **目标字区**：大字居中，下方一行三段：拼音（灰）· 小鹤码（等宽、加粗、
  Bone 色）· 引导语「① 先按 U（sh）② 再按 L（uang）」。
- **输入条 Inbox**：等宽字体 28px，无框、仅底部 2px Whisper Line，聚焦时
  底线变 Cinnabar。错字整条轻晃（translateX ±4px，120ms）。
- **按钮**：主按钮 Cinnabar 填充、Bone 字、圆角 12px、按下 -1px；
  次级按钮 ghost（Whisper Line 描边）。无外发光。
- **卡片**：圆角 16px，Ink Surface，仅当层级需要时使用；统计页高值密度
  区域改用 border-top 分隔。
- **进度条**：2px 高，Bamboo 填充，无动画条纹。
- **空状态**：错词本空 = 一幅简笔鹤形 SVG + 「还没有错过，继续」；
  导入空 = 虚线拖放区示意文件图标。
- **加载**：导入解析时骨架条（与报告行同高）闪烁，不用圆环。

## 5. Layout Principles

- 顶栏：左「鹤练」字样标（Outfit 700，朱砂点收尾），中为六视图导航
  （练习/课程/导入/统计/错词本/设置），右为连续天数徽标。56px 高，
  Whisper Line 下边线。
- **练习视图**（核心）：max-width 880px 中轴。纵向节奏：目标字区 →
  输入条 → 状态行（时间·准确率·速度·进度）→ 键盘图。键盘图固定视口下方，
  练习中不随滚动消失。
- **课程视图**：左侧五阶纵列（当前阶 Cinnabar 左缘 2px），右侧当前阶内容区。
  键位全景图：26 键四行标准键盘布局，每键标注声母/韵母双角色，
  zh/ch/sh 三键朱砂描边。
- **导入视图**：左右分栏——左拖放区+格式说明四卡（userdb.txt / dict.yaml /
  纯文本 / custom_phrase），右解析报告（条数、去重、切分失败行可展开）。
- **统计视图**：上方三枚等宽数字（总时长/总会话/平均准确率，JetBrains Mono
  大号），中部 26 键热力键盘（错误率 0→Rust 渐变），下方最近 100 会话
  准确率折线（内联 SVG）。
- 全站 max-width 1100px 居中；分栏用 CSS Grid；<768px 全部单列，键盘图隐藏。
- 触控目标 ≥44px。

## 6. Motion & Interaction

- 所有过渡：`cubic-bezier(0.22, 1, 0.36, 1)` 150–200ms；仅 transform/opacity。
- 答对：目标字 60ms 淡出上移 8px，下一字淡入（打字节奏优先，总时长 <100ms）。
- 答错：输入条水平轻晃；错键 Rust 闪烁；不做弹层打断。
- 会话结束：结果卡从下方 16px 淡入，统计数字 300ms 级联浮现（stagger 60ms）。
- 键盘图 ①② 切换：键帽填充色 120ms 过渡。
- 禁止：无限循环的装饰动画（打字站要安静）、布局属性动画。

## 7. Anti-Patterns（禁行清单）

- 无 emoji（①② 为 Unicode 圈数字，属排版符号允许）
- 无 Inter / 无纯黑 #000000 / 无霓虹辉光 / 无 >80% 饱和色
- 无三等分卡片行（导入页四卡用 2×2 不对称权重：userdb 卡占双倍高）
- 无 "Elevate/Seamless" 式文案；中文文案直接、动词开头
- 无居中式营销 Hero——练习主屏的"居中"是仪表布局，文案口号一律不要
- 无假数据圆整（统计显示真实小数）
- 无占位名；无失效外链图片（全站仅 Google Fonts 一个外链）

---

## 设计评审记录（2026-08-30，自查通过）

对照 stitch-design-taste 九节逐条检查：
1. 氛围有明确主张（深夜书法房），密度/变化/动效三轴数值化 ✓
2. 单强调色 Cinnabar（饱和度 63%<80%），辅助色 Bamboo 为低饱和功能色非第二强调 ✓
3. 字体：Outfit + JetBrains Mono + 思源宋体（教学衬线，非装饰）✓ 无 Inter
4. 组件全部定义交互态（键帽四态、按钮两式、空态有构图）✓
5. 布局 Grid、max-width 收束、无重叠、键盘图固定策略明确 ✓
6. 动效仅 transform/opacity，级联有时序，无布局动画 ✓
7. 响应式：<768px 单列、触控 44px、键盘图移动端隐藏（练习依赖物理键盘）✓
8. 反模式清单逐条可执行 ✓
9. 与产品约束一致：桌面优先、练习专注、零基础教学（目标字宋体大字 + ①②引导）✓
