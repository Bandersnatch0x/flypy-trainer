# Design System: 鹤练 FlypyTrainer（小鹤双拼练习站）

> 来源：stitch-design-taste 方法论（Stitch MCP 未挂载，人工执行）。
> 定稿前已通过反模式评审与 UI/UX 深度重构规范。

## 1. Visual Theme & Atmosphere

深夜书法房的气质：墨色底、一点朱砂。练习时注意力必须钉在中央目标字上，
其余一切退为低语。
- **密度（Density）**：4（日常应用平衡档，组件具呼吸感且信息组织紧凑）。
- **变化度（Variance）**：6（练习主屏严格中轴对称——它是"工具仪表"不是 Hero 着陆页，对称是刻意决定而非懒惰；其他视图采用结构化网格分栏）。
- **动效（Motion）**：4（流体但克制，打字是高频微交互，动效响应时间控制在 60–200ms 内，绝不拖泥带水）。
- **质感层级（Elevation & Craft）**：避免扁平死黑，运用墨玉与砚台的温润景深——以微内阴影 (`inset 0 1px 0 rgba(255,255,255,0.06)`)、微环境漫反射 (`box-shadow: 0 4px 20px rgba(0,0,0,0.3)`)、键帽物理倒角与微高光，营造沉静高级的工具质感。
- **响应策略**：桌面优先；<768px 单列降级、移动端保留可点按键盘（弹性键宽、行结构与键位不变、键高 ≥44px；键盘矩阵键宽为 44px 触控基准的成文例外，SPEC-0004 §4-D1）。

## 2. Color Palette & Roles

- **Ink Canvas** (`#101014`) — 全站背景，深邃墨色底（非死黑 #000000），辅以微妙的径向氛围光。
- **Ink Surface** (`#17171C`) — 卡片/容器/键帽基础底面，带 1px 细微光边。
- **Ink Raised** (`#1F1F26`) — 悬停/激活态、按键按压、次级容器底色。
- **Whisper Line** (`rgba(255, 255, 255, 0.08)`) — 1px 分隔线、结构描边与微高光。
- **Bone Text** (`#EDEDEF`) — 主文字（目标字、标题、激活项、等宽关键数据）。
- **Ash Text** (`#8B8B93`) — 次级文字、辅助说明、标签元数据。
- **Cinnabar** (`#D96C4F`) — 唯一主强调色：当前要按的键 ①、主按钮、聚焦底线、连练徽标点缀。饱和度约 63%。
- **Bamboo** (`#7FA98C`) — 唯一功能辅助色：下一待按键 ②、正确计数、连续天数、进度条。低饱和灰绿（饱和度约 40%）。
- **Rust** (`#C4574E`) — 错误反馈、错词统计、错键热力高值（与 Cinnabar 同族低刺激）。

禁令：无紫/蓝霓虹、无渐变彩虹辉光、全站无第二种高饱和色、无纯黑 #000000。

## 3. Typography Rules

- **目标字（Display）**：`"Source Han Serif SC", "Noto Serif SC", "SimSun", serif` 思源宋体，
  字重 600，`clamp(4rem, 12vw, 7rem)`——练习的焦点必须是汉字本体，宋体笔锋
  帮助零基础者看清结构与间架；这是全站唯一衬线使用，属教学核心用途而非装饰。
- **UI 文字（Body）**：`"Outfit", "PingFang SC", "Microsoft YaHei", sans-serif`，
  行高 1.6，正文最大 65ch。Outfit 经 Google Fonts 引入（唯一外链）。
- **码与数字（Mono）**：`"JetBrains Mono", Consolas, monospace`——小鹤码、统计数字、
  计时器、按键字母一律等宽。
- **键帽字母**：JetBrains Mono 600，18px；键帽下方韵母 Outfit 400，9.5px。
- **禁令**：无 Inter、无花哨艺术渐变字。

## 4. Component Stylings

- **键帽 Key**：48×48px，圆角 10px，Ink Surface 底 + Whisper Line 描边，具有砚台物理质感：
  - *默认*：顶部微高光 (`linear-gradient(180deg, #1C1C22 0%, #15151A 100%)`) + 微投影 (`0 3px 6px rgba(0,0,0,0.35)`)。
  - *① 待按*：Cinnabar 填充 + 右上角精致 ① 角标 + 朱砂微环境光。
  - *② 待按*：Bamboo 边框 + Bamboo 字色 + 右上角 ② 角标。
  - *按下*：`transform: translateY(2px) scale(0.98)` + 亮度微调。
  - *错按*：Rust 闪烁 120ms。
  - *特殊键（V/I/U 翘舌键）*：常态保留半透明朱砂微光边框与标识。
- **练习控制栏（Modes & Hint）**：
  - *模式切换*：胶囊式分段控制器（Segmented Pill Bar），平滑圆角，激活项带有清晰的边框与微光提升。
  - *提示层级*：自定义暗色 Radio 单选组件，优雅圆点指示，消除原生粗糙控件。
- **目标字区与输入框**：
  - *目标字*：居中呈现，沉静而有呼吸感。
  - *提示与引导*：胶囊化微排版，拼音与编码对比清晰，①② 引导用朱砂/竹绿标签分段。
  - *输入槽*：极简底线配合呼吸聚焦容器，错字水平弹性晃动 (translateX ±4px，120ms)。
- **状态仪表栏（Status Row）**：
  - 模块化微卡片（Mini Metric Pills），等宽大数字搭配次级标签，数据一目了然。
- **进度条（Progress）**：
  - 3px 高，Ink Raised 轨道槽 + Bamboo 柔和微光填充。
- **卡片（Cards）**：
  - 圆角 16px，Ink Surface 底色，配以 `inset 0 1px 0 rgba(255,255,255,0.06)` 微高光内边与轻柔阴影。
- **按钮（Buttons）**：
  - 主按钮 Cinnabar 填充、Bone 字、圆角 12px、按下微位移；次级按钮 ghost（Whisper Line 描边，hover 时骨色文字）。
- **空状态（Empty States）**：
  - 错词本空：简笔鹤形 SVG + 朱砂点睛 + 「还没有错过，继续」；
  - 导入空：虚线拖放区配合清晰的文件格式标签与点击上传入口。
- **结算弹窗（Result Modal）**：
  - 毛玻璃全屏背景 (`backdrop-filter: blur(12px)`)，居中精致墨色卡片，四项关键指标级联浮现，主按钮聚焦引导。

## 5. Layout Principles

- **顶栏 Topbar**：左「鹤练」字样标（Outfit 700，朱砂点收尾），中为六视图导航（练习/课程/导入/统计/错词本/设置），右为连续天数徽标。56px 高，毛玻璃吸顶，Whisper Line 下边线。
- **练习视图（核心）**：max-width 880px 严格中轴。纵向节奏：模式与提示控制 → 目标字区 → 输入槽 → 状态仪表行 → 进度条 → 键盘图。键盘图固定视口下方，练习中不随滚动消失。
- **课程视图**：左侧五阶纵列导航（当前阶朱砂左指示与微高亮），右侧当前阶内容卡片。阶段 0 键位全景图，阶段 1 韵母操练矩阵。
- **导入视图**：四格式卡片不对称权重网格（userdb.txt 设为推荐徽标），下方大尺寸拖拽上传槽，底部解析报告与词库管理列表。
- **统计视图**：顶部三大等宽数据卡片（总时长/总会话/平均准确率），中部错键热力图（错误率 0→Rust 渐变），下方准确率折线图（SVG 曲线与渐变区域），底部会话历史列表。
- **错词本与设置**：卡片化布局，设置项配备美化 Custom Checkbox/Radio 开关，危险操作区独立高辨识度警示卡片。
- 全站 max-width 1100px 居中；<768px 响应式单列降级。
- 触控目标 ≥44px（成文例外：移动端键盘矩阵按「高」执行——键高 ≥44px 保触达面积，键宽弹性可低至 ~24px，键宽 <28px 隐角标保主字母，SPEC-0004 §4-D1/D3）。

## 6. Motion & Interaction

- 所有过渡：`cubic-bezier(0.22, 1, 0.36, 1)` 120–200ms；仅 transform/opacity/background-color。
- 答对：目标字 60ms 淡出上移 8px，下一字淡入（打字节奏优先，总时长 <100ms）。
- **击键体感与震荡反馈（打砖块打击感）**：开启后，按键命中时键帽触发 160ms 机械轴弹性回弹与瞬态微缩放，伴随朱砂/墨色微粒子与冲击波环消散；舞台区产生 1.2px 极轻微反冲震荡；词条完成时汉字中心迸发结题微光。用户可在「设置」中随时开启/关闭。
- 答错：输入条水平轻晃；错键 Rust 闪烁；不做阻断弹层。
- 会话结束：结算卡从下方 16px 淡入，统计数字 300ms 级联浮现（stagger 60ms）。
- 键盘图 ①② 切换：键帽色彩 120ms 流体过渡。
- 禁止：无限循环的跑马灯或炫目光效（打字站需保持专注静心）。

## 7. Anti-Patterns（禁行清单）

- 无 emoji（①② 为 Unicode 圈数字排版符号，属于教学引导）
- 无 Inter / 无纯黑 #000000 / 无霓虹辉光 / 无 >80% 饱和色
- 无三等分死板卡片行（导入页四卡 2×2 权重分明）
- 无 "Elevate/Seamless" 式空泛文案；中文文案直接、动词开头
- 无居中式营销 Hero——练习主屏的"居中"是仪表仪表盘布局，绝无宣传口号
- 无假数据圆整（统计显示真实数字与真实小数）
- 无占位名；无失效外链图片（全站仅 Google Fonts 一个外链）

## 平台例外：微信小程序配色（2026-08-30 用户裁定）

网页版基准为「深夜书法房」（Cinnabar 单强调色）。微信小程序端按用户裁定的
Stitch 设计稿「Obsidian Focus」独立配色，不适用上述禁令中的强调色约束：

| 令牌 | 值 | 角色 |
| --- | --- | --- |
| --bg | #131313 | 页面底 |
| --surface | #1A1A1A | 卡面 |
| --stroke | #2A2A2A | 描边 |
| --accent | #00F2FF | 强调（tabBar/开关/主按钮） |
| --ok | #00E676 | 正确反馈 |
| --err | #FF1744 | 错误反馈 |
| --ghost | #4A4A4A | 次要文字 |

热力图仍用 Rust 系 rgba(196,87,78)；其余纪律（无假数据、无营销文案、
无外链、中文直陈）两端一致。本例外只覆盖 `miniprogram/`，
网页版改动仍按「深夜书法房」基准评审。

---

## 设计评审记录（2026-08-30，UI/UX 深度重构通过）

对照 stitch-design-taste 九节与 UI/UX 体验质感逐条自查：
1. 氛围有明确主张（深夜书法房、墨色底、一点朱砂），三轴数值化（密度 4 / 变化 6 / 动效 4）✓
2. 单强调色 Cinnabar，辅助色 Bamboo 为低饱和功能色，Rust 为错误指示，严格遵守单强调色纪律 ✓
3. 字体：Outfit + JetBrains Mono + 思源宋体（教学衬线），无 Inter ✓
4. 组件全面升级：机械键帽微倒角与物理手感、自定义暗色表单控件、胶囊分段导航、仪表盘微卡片、毛玻璃弹窗 ✓
5. 布局 Grid + Flexbox、max-width 收束、键盘图固定策略与中轴对称明确 ✓
6. 动效专注高效，打字节奏优先 (<100ms)，无布局属性重排动画 ✓
7. 响应式：<768px 单列、触控 44px、移动端保留可点按键盘（键盘矩阵键宽成文例外）✓
8. 反模式禁行清单严格执行（无 emoji、无纯黑、无霓虹、无营销文案）✓
9. 与产品约束 100% 保持一致：不改变任何 DOM id 与 class，保证测试与逻辑零破坏 ✓
